--- ISSUE 831 ---
Title: Friction: FAQ_OBJECTIONS.md — stale refs: validate-spec cmd, docs/ci/ path, dist/*.whl, CHERENKOV_LLM_PROVIDER/LOCALAI_BASE_URL env vars
Body:
Part of #816 — M1 onboarding asset friction log (cold-run audit of onboarding docs, 2026-08-01).

## Path
`docs/onboarding/FAQ_OBJECTIONS.md` — several stale tool/path references that would mislead M1 practitioners:

1. **Line 17:** "Run `cherenkov validate-spec --spec swagger.json`" — no such command exists. `./bin/cherenkov --help` lists only `validate` (plus audit/check-stale/etc.). Suggested fix: `cherenkov validate --spec <file>` or remove.
2. **Line 73:** "a template is in `docs/ci/`" — no `docs/ci/` directory exists. The workflow template lives at `.github/workflows/cherenkov.yml`. Suggested fix: correct the path.
3. **Line 101:** "Install CHERENKOV from the prebuilt wheel (`dist/cherenkov-1.0.0.whl`)" — no `dist/` directory ships in the repo. Suggested fix: point to `pip install .` / `pip install -e .` (the package is not on PyPI yet — M2-gated), or to the CI-built artifacts.
4. **Lines 25/101:** env vars `CHERENKOV_LLM_PROVIDER=openai` / `CHERENKOV_LLM_PROVIDER=localai` / `LOCALAI_BASE_URL=...` — none of these are read by `cherenkov/core/config.py` (which uses `OLLAMA_URL`, `GEN_MODEL`, `CHERENKOV_VLM_LOCALAI_URL`, tier `[substrate.tiers.*]` keys in `cherenkov.toml`). The local default is now **Ollama**, not LocalAI. Suggested fix: refresh the config/env-var docs to match `cherenkov/core/config.py` and the `cherenkov.toml` produced by `init`.

## Suggested fix
Batch-audit FAQ_OBJECTIONS.md against the current CLI (`./bin/cherenkov --help`) and config schema before M1; the four items above are the ones found in this pass.


========================================
--- ISSUE 830 ---
Title: Friction: session_a_zero_to_hero.md Act 2 — init transcript shows petstore.json autodetect; real init detects mut_spec.json/stub/target_spec.json (timeline contradiction)
Body:
Part of #816 — M1 onboarding friction log (cold-run audit of session A, executed 2026-08-01 on a clean `main` clone in a fresh venv).

## Path
`docs/onboarding/sessions/session_a_zero_to_hero.md` — Act 2, `./bin/cherenkov init` (line 56) + transcript (lines 61–73).

## Evidence — transcript vs actual behavior
The doc's transcript shows:
```
Auto-detecting OpenAPI specifications...
Found: petstore.json (auto-matched spec target)
Creating configuration profile: autodetect (laptop)
Writing cherenkov.toml...
✓ cherenkov.toml created successfully.
```
A real run (after fixing the dependency issue; fresh checkout, only the doc's steps executed) produces:
```
  [3/4] Spec files: 2 found
         - mut_spec.json
         - stub/target_spec.json
  [4/4] Profile:    laptop
  [OK] Generated /tmp/.../cherenkov_onboarding/cherenkov.toml
```
Two mismatches:
1. **No `petstore.json` is found.** Real autodetect picks up `mut_spec.json` and `stub/target_spec.json` (files shipped in the repo). `petstore.json` is only downloaded in Act 3, *after* init — so the transcript's "Found: petstore.json" can never be true even in principle (timeline contradiction: init runs before curl in the doc's own order).
2. **No "Writing cherenkov.toml... ✓ cherenkov.toml created successfully." banner.** Real output is `[OK] Generated .../cherenkov.toml`, and a "Next steps" block (`./bin/cherenkov doctor`, `./bin/cherenkov validate --target <url>`) that the doc doesn't show. Also the created `cherenkov.toml` registers `mut_spec.json`/`stub/target_spec.json` as sources — not `petstore.json` — which may surprise a practitioner who then runs plain `validate` without `--spec`.

## Suggested fix
Update the Act-2 transcript to real init output (or annotate as illustrative), and move the `curl` download of `petstore.json` BEFORE `cherenkov init` if the intent is for init to auto-detect the petstore spec — or pass `--spec petstore.json` explicitly at validate/generate time as the doc already does.


========================================
--- ISSUE 829 ---
Title: Friction: session_a_zero_to_hero.md Act 4 — validate runs shipped fixture specs (15/16 fail); transcript report format/4-failure narrative does not reproduce
Body:
Part of #816 — M1 onboarding friction log (cold-run audit of session A, executed 2026-08-01 on a clean `main` clone in a fresh venv).

## Path
`docs/onboarding/sessions/session_a_zero_to_hero.md` — Act 4, `./bin/cherenkov validate --target https://petstore3.swagger.io/api/v3 --spec petstore.json` (line 140) + transcript (lines 145–222).

## Evidence — actual output vs transcript
Real run (after the setup fixes; ~40 s) ends with:
```
Results: 1/16 passed  [SUCCESS]
  FAIL  missing_photoUrls  Error: expect(received).toBe(expected) // Object.is equality
  FAIL  demo_deleted      Error: ...
  FAIL  unauthorized      Error: ...
  FAIL  demo_hallucinated Error: ...
  FAIL  demo_correct_q    Error: ...
  FAIL  demo_correct      Error: ...
  FAIL  missing_name      Error: ...
  FAIL  happy_path        Error: ...
  FAIL  golden_deleted    Error: ...
  FAIL  demo_weakened_v   Error: expect(received).toHaveProperty(path)
  FAIL  demo_weakened     Error: ...
  FAIL  demo_tighten      Error: ...
  FAIL  password_too_short Error: ...
  FAIL  demo_deleted_v    Error: ...
  FAIL  golden_correct    Error: ...
```
Exit code 0.

## Why (root cause for the noise)
`validate` runs **every** `*.spec.ts` in `stub/generated_tests/`, which — on a fresh checkout — already contains 13 shipped fixture files (`demo_*`, `golden_*`, `password_too_short`, `_scores.json`) used by the "catch the AI cheating" demos. Those fixtures are intentionally mutated assertions and fail against the live petstore3, drowning out the 4 genuinely generated tests. A practitioner sees "1/16 passed" with zero explanation.

## Doc-transcript mismatches
1. Expected "CHERENKOV VALUE ASSERTION TIGHTENING REPORT" header, 4 named scenarios with exact assertion lines (`expect(response.status).toBeLessThan(500)` etc.) — none of this format exists today; the real output is `Results: N/16 passed` + playwright failure lines.
2. The transcript's 4 failures (post_pet_missing_photourls / get_store_inventory / get_pet_by_id_zero / get_user_login_headers) cannot reproduce: `get_store_inventory` is skipped by generate (low richness), and the other filenames are never produced (see the generate friction issue).
3. Transcript's "Git status verification: ✓ Git status is 100% clean — zero test files were auto-modified" section does not appear in real output at all. (FWIW, `validate` does re-write each scenario file before running it — see PLAYWRIGHT "wrote generated test file" log lines — so the D7 "clean" claim needs re-checking too.)
4. Spurious warning at start: `warn: Unrecognised OpenAPI version: '3.0.4'. Known: ['2.0', '3.0.0', ...'3.1.0']` — petstore3 serves a valid OpenAPI **3.0.4** spec; the validator's known-version list is stale (missing 3.0.4).
5. Exit code is 0 even with 15/16 failing — the doc never mentions `--fail-on-drift`; a practitioner may not notice failures in CI semantics.

## Suggested fix
1. Product: `validate` should only run the scenarios it generated (or the doc should point `--output-dir` to a fresh dir / clean `generated_tests` first). At minimum, document that fixture files in `generated_tests` are picked up.
2. Doc: re-record/rewrite the Act-4 transcript to match current output format, or annotate it as "illustrative output from an earlier version".
3. Product: add `3.0.4` to the accepted OpenAPI versions (or switch to a semver-tolerant check).


========================================
--- ISSUE 828 ---
Title: Friction: session_a_zero_to_hero.md Act 3 — generate reports 38/38 but only 4 files persist (scenario-name overwrite); transcript filenames never produced
Body:
Part of #816 — M1 onboarding friction log (cold-run audit of session A, executed 2026-08-01 on a clean `main` clone in a fresh venv).

## Path
`docs/onboarding/sessions/session_a_zero_to_hero.md` — Act 3, `./bin/cherenkov generate --spec petstore.json --output-dir stub/generated_tests --no-repair` (line 98) + transcript (lines 103–122).

## Evidence
Command runs (after the setup fixes in the other friction issues) and prints:
```
Planned 38 scenarios. Handing off to AI Generator...
  Mode: single-pass (--no-repair)
...
Successfully generated 38/38 test suites.
Output located in stub/generated_tests/
```
But only **4** `.spec.ts` files exist afterwards:
```
stub/generated_tests/happy_path.spec.ts
stub/generated_tests/missing_name.spec.ts
stub/generated_tests/missing_photoUrls.spec.ts
stub/generated_tests/unauthorized.spec.ts
```
The generator names output files by **scenario** (happy_path / unauthorized / missing_name / missing_photoUrls), and the same four scenario names are reused for every endpoint (17 endpoints × ~2–3 scenarios). Each endpoint silently **overwrites** the previous endpoint's file, so "38/38" is misleading — only the last endpoint's scenarios survive.

## Doc-transcript mismatch
The transcript shows per-endpoint filenames that are never produced:
```
✓ Generated post_pet_missing_photourls.spec.ts
✓ Generated get_store_inventory.spec.ts
✓ Generated get_pet_by_id_zero.spec.ts
✓ Generated get_user_login_headers.spec.ts
```
None of these exist after a real run. Also "32 endpoints detected" vs actual 17 endpoints (38 scenarios), and the real log line is `Ingesting OpenAPI spec: petstore.json` (no "32 endpoints detected"). Note `/store/inventory` is skipped with "skipping low richness endpoint" — so the Act-4 narrative endpoint `get_store_inventory` is never generated at all.

## Suggested fix
1. Product: include the endpoint path in generated filenames (e.g. `post_pet_missing_photourls.spec.ts` as the transcript claims) so files don't collide; or make `generate` report the true persisted file list.
2. Doc: refresh the Act-3/Act-4 transcripts to match real output (endpoint count, filenames, log lines) after the product fix.


========================================
--- ISSUE 827 ---
Title: Friction: session_a_zero_to_hero.md — no workspace provisioning or prerequisites section (no clone/install/Node/Ollama steps)
Body:
Part of #816 — M1 onboarding friction log (cold-run audit of session A, executed 2026-08-01 on a clean `main` clone in a fresh venv).

## Path
`docs/onboarding/sessions/session_a_zero_to_hero.md` — Act 2 (starts line 30), whole session.

## What's missing (blocker for a cold, unaided run)
The doc opens with the visual "[A clean, empty WSL terminal screen inside `/home/moaid/cherenkov_onboarding`]" and immediately starts issuing commands. There is **no provisioning step** — a cold practitioner is never told:

1. **Where the workspace comes from.** No `git clone`, no download, no link. `target/`, `stub/`, and `bin/` only exist inside the repo checkout (`https://github.com/moaidmoatasem/cherenkov-qa`). A practitioner handed this doc alone cannot obtain the workspace.
2. **How CHERENKOV itself is installed.** No `pip install -e .` / `pip install -r requirements.txt` (see the init-crash friction issue — the doc's only pip step installs the wrong requirements file).
3. **Node.js prerequisite.** `npm install` / `npx playwright install` (lines 45–46) require Node 18+ and npm; never stated. (Verified OK on Node v22.23.0 / npm 10.9.8.)
4. **Ollama prerequisite.** Act 3's `cherenkov generate` routes to a local LLM (`qwen2.5-coder:7b` via Ollama at `localhost:11434`; confirmed from `cherenkov.toml` `[substrate.tiers.small]` produced by `init`, and `cherenkov/core/config.py` `OLLAMA_URL`/`GEN_MODEL`). The doc's presenter script says "our local LLM—a local Qwen 2.5 coder model" but no step installs/starts Ollama or pulls the model. On a cold machine `generate` will time out / fail (I observed the first LLM call took ~25 s only because Ollama was already running; `init` emits a non-fatal "Ollama model warm-up failed" WARN when it isn't reachable).
5. **No prerequisites summary / no links** to `docs/onboarding/FAQ_OBJECTIONS.md` or `run_demo.sh` — nothing a beginner can fall back on.

## Suggested fix
Add a short "Prerequisites & workspace setup" block before Act 2:
```bash
# e.g.
git clone https://github.com/moaidmoatasem/cherenkov-qa.git cherenkov_onboarding
cd cherenkov_onboarding
python3 -m venv .venv && source .venv/bin/activate
pip install -e .
# Node.js 18+ required; Ollama running with qwen2.5-coder:7b (or set CHERENKOV_* env)
```
And update the Act-2 visual/voiceover to match a real cold-start flow (or explicitly state the workspace is pre-provisioned for the recording).


========================================
--- ISSUE 826 ---
Title: Friction: session_a_zero_to_hero.md Act 2 — \cherenkov init\ crashes: ModuleNotFoundError httpx (wrong requirements file, tool never installed)
Body:
Part of #816 — M1 onboarding friction log (cold-run audit of session A, executed 2026-08-01 on a clean `main` clone in a fresh venv).

## Path
`docs/onboarding/sessions/session_a_zero_to_hero.md` — Act 2, step 1 (lines 37–48), then step `./bin/cherenkov init` (line 56).

## Exact command (verbatim from doc)
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r target/requirements.txt
...
./bin/cherenkov init
```

## What happens
`pip install -r target/requirements.txt` installs only the **target demo API's** deps (`fastapi`, `uvicorn`, `pydantic`). CHERENKOV's own runtime deps (`httpx`, `requests`, `pyyaml`, `memsearch`, `jinja2`, …) are never installed, and the doc never installs the tool itself (no `pip install cherenkov-qa` — the package is **not on PyPI** (M2-gated) — and no editable install). The very first CLI invocation crashes:

```
$ ./bin/cherenkov init
Traceback (most recent call last):
  File "/tmp/practitioner-run/cherenkov_onboarding/cherenkov/cli/core.py", line 117, in <module>
    main()
  File "/tmp/practitioner-run/cherenkov_onboarding/cherenkov/cli/core.py", line 112, in main
    _register_commands()
  File "/tmp/practitioner-run/cherenkov_onboarding/cherenkov/cli/core.py", line 25, in _register_commands
    from cherenkov.cli.commands.audit import audit_cmd
  File "/tmp/practitioner-run/cherenkov_onboarding/cherenkov/cli/commands/audit.py", line 20, in <module>
    import httpx
ModuleNotFoundError: No module named 'httpx'
```

## Verified fix
`pip install -e .` in the venv (or `pip install -r requirements.txt` — the repo-root one, which is the real dep set) resolves it; `init` then completes. The repo-root `requirements.txt` even says it is the "single source of truth" for deps — the doc just points at the wrong file.

## Suggested fix
1. In Act 2, replace `pip install -r target/requirements.txt` with the real install: `pip install -e .` (after the workspace provisioning step — see related issue on missing prerequisites).
2. Add a note that the target API deps (`target/requirements.txt`) are only needed for the local demo harness (Act 4 targets the public petstore3, so a practitioner following the session doesn't need them at all — installing them first is actively misleading).
3. Re-record/update the session transcript if the recording shows a working `init` without these steps.


========================================
--- ISSUE 819 ---
Title: fix(tests): test_verify_cmd mocks drift from E0.5i run_proof kwargs (pre-existing, found by T-track swarm)
Body:
Pre-existing failures on main at ea555e7, found independently by 5 swarm agents working #810/#811/#812/#814/#815.

- tests/unit/test_verify_cmd.py::TestVerifyCmd::test_no_divergences_exits_0
- tests/unit/test_verify_cmd.py::TestVerifyCmd::test_llm_flag_passed

Root cause: mocked call expects run_proof(base_url, spec, use_llm, max_probes, probed_endpoints) but verify.py now passes known_identifiers=None, allow_mutations=False (added in E0.5i, commit 886648c). D7 forbids agents auto-editing test code, so this needs a human/SDET owner.

========================================
--- ISSUE 816 ---
Title: Prep onboarding assets ahead of M1 recruitment
Body:
Tech-debt track T2 in `docs/ROADMAP_2026H2.md` ("Record onboarding assets — do this during M1's recruitment wait").

M1 (`docs/ROADMAP_2026H2.md`, window 2026-08-12 → 2026-08-26) needs ≥3 external practitioners to complete `docs/onboarding/sessions/session_a_zero_to_hero.md` cold, unaided, timed. That milestone itself is human-owned and can't be done by an agent — but the *prep* work is fair game right now:

1. Walk through `docs/onboarding/sessions/session_a_zero_to_hero.md` yourself end-to-end on a clean checkout, exactly as a cold practitioner would, and file friction-log issues for anything unclear, broken, or missing (this is explicitly the deliverable per M1's redesigned exit criterion — friction logs, not just pass/fail).
2. Confirm every command in the doc actually runs against the current `main` (things drift — the doc may reference flags/commands that changed since it was written).
3. Do **not** recruit or simulate practitioners yourself, and don't mark M1 checkboxes in `docs/ROADMAP_2026H2.md` — that requires real people outside this repo.

========================================
--- ISSUE 815 ---
Title: Consolidate dual AI routing layers (ai/ + substrate/)
Body:
Tech-debt track T7 in `docs/ROADMAP_2026H2.md`.

Two provider-routing layers currently coexist: `cherenkov/ai/` (e.g. `openai_client.py`, `nemoclaw_client.py`) and `cherenkov/substrate/providers/`. This is real debt (duplicated retry/timeout/error-handling logic, two places to add a new provider) but is *not* a milestone blocker — do not let this expand into a rewrite.

Scope for a first pass: map every call site of both layers (`grep -rn "from cherenkov.ai\|from cherenkov.substrate"`), identify which one is actually the "current" path vs. legacy, and propose (as a PR description, not necessarily executed in one PR) a consolidation plan. If the two layers serve genuinely different purposes (e.g. one is local-model-only, the other cloud), document that distinction explicitly in a module docstring instead of merging — don't force a merge that doesn't make architectural sense.

========================================
--- ISSUE 814 ---
Title: Retire root cherenkov.py (migration, not a delete)
Body:
Tech-debt track T1 in `docs/ROADMAP_2026H2.md`.

Root `cherenkov.py` has 8 load-bearing consumers that will break if it's simply deleted (confirmed by a prior premature-delete incident, `0f16fed`, which broke CI and Docker and had to be restored in #675):
- `ci.yml:612-626`
- `Dockerfile.mcp`
- `bin/cherenkov-npm.js:42`
- `setup_oi.sh`
- `qwen-code-integration.sh`
- `package.json`
- `scripts/ci_docs_check.py`
- `scripts/check_cli_docs.py`

Scope: migrate each consumer to the real entrypoint (whatever root `cherenkov.py` is currently a shim/compat-layer for — check `cherenkov/cli/__main__.py` or equivalent first), verify each consumer still works, then remove the root shim. Do this consumer-by-consumer with a green CI run after each, not as one big-bang change — the whole point of this ticket existing is that a big-bang delete already broke things once.

========================================
--- ISSUE 812 ---
Title: Deepen MCP tool surface (check-suite/verify/generate as agent-invokable tools) and publish per #792
Body:
Follow-up from `docs/reviews/COMPETITIVE_POSITIONING_2026-08.md`.

There is a real MCP server (`cherenkov/mcp/{server.py,protocol.py,handlers.py,auth.py,mesh_router.py,policy.py}` + `cherenkov/mcp/tools/{sentinel.py,teleport.py}`), so this is *not* a "build an MCP server" task — it's a depth/registration gap:

1. Confirm (or add) MCP tool definitions that expose `cherenkov check-suite`, `cherenkov verify`, and `cherenkov generate` as directly callable tools an external agent (Claude Code, Cursor) can invoke mid-session — e.g. "I just changed this endpoint, did I weaken any assertions?" — rather than only via CLI subprocess.
2. Registry publish (#792): no `server.json`/registry manifest exists yet; `cherenkov/mcp/marketplace/registry.py` only *consumes* a marketplace via hardcoded `_stub_tools()` — it doesn't publish anything. `smithery.yaml` is present and points at `cherenkov mcp serve` correctly per the 2026-07-31 handover note, but hasn't been submitted to the actual Smithery/MCP registry.

This is the single highest-leverage item that every piece of external competitive research (see the positioning doc) converged on independently — worth prioritizing over any new Phase 15/16 work.

========================================
--- ISSUE 810 ---
Title: Wire Enterprise SAML/RBAC CLI placeholders to the real logic modules
Body:
Follow-up from the 2026-08-01 issue triage on #755 / #757.

Both `cherenkov/enterprise/saml.py` (140 lines, real XML parsing) and `cherenkov/enterprise/rbac.py` (151 lines, real engine) exist and work in isolation, but:

- `cli/commands/enterprise.py:89-92` `saml_configure` is literally `"""Placeholder for SAML configuration."""` — not calling into `saml.py` at all.
- `cli/commands/enterprise.py:100-103` `rbac_assign` is literally `"""Placeholder for RBAC assignment."""` — not calling into `rbac.py`, and RBAC is not enforced anywhere in `web/middleware` or `web/auth`.

Scope: wire the two CLI commands to the real modules, and decide (separately, needs a design call) whether RBAC enforcement belongs in `web/auth/deps.py`'s existing `require_role` path or a new middleware layer — don't invent persistence/API surface beyond what's asked here.

========================================
--- ISSUE 809 ---
Title: Release hygiene: publish v1.2.0 GitHub Release, resolve malformed v.1.1.1 tag, clean up orphan docs version
Body:
Follow-up from #807 (merged) and `docs/ROADMAP_2026H2.md` M2's "Tag hygiene first" bullet.

**Fixed already (#807):** `.release-please-manifest.json` and `package.json` now read `1.2.0`, matching `pyproject.toml` and the `v1.2.0` git tag.

**Still open:**
1. **No GitHub Release exists for the `v1.2.0` tag.** `docs-deploy.yml` only runs `mike deploy --update-aliases <ver> latest` on `release: published`, so the live docs site's `/latest/` alias is still pointing at old (pre-1.2.0) content. Needs: publish a GitHub Release for `v1.2.0` (this triggers the workflow automatically), or manually run from `docs-site/`: `mike deploy --push --update-aliases 1.2 latest && mike set-default --push latest`.
2. **The `v1.1.1` release is tagged `v.1.1.1`** (stray dot) — inconsistent with every other tag (`v1.0.0`, `v1.1.0`, `v1.2.0`). This is what silently broke `docs-deploy.yml`'s `cut -d. -f1,2` version-extraction step in the first place. Deleting/retagging a *published* release tag is a judgment call (rewrites a historical reference) — flag for a human decision rather than doing it unilaterally.
3. **Orphan `1.1.1` version directory on `gh-pages`** (`git ls-tree -d --name-only origin/gh-pages` → `1.0`, `1.1`, `1.1.1`, `dev`) — doesn't match the major.minor scheme every other version uses. Once (1) is resolved, `mike delete 1.1.1 --push` from `docs-site/` cleans this up.

Going forward, cut releases through `release-please` (manifest is now correctly seeded) rather than hand-tagging, to avoid a repeat.

========================================
--- ISSUE 792 ---
Title: Integration: Publish MCP server to official registry
Body:
Part of EPIC #790

========================================
--- ISSUE 790 ---
Title: EPIC: Sprint 4 Integrations
Body:
AI Ecosystem and Onboarding integrations.

========================================
--- ISSUE 789 ---
Title: Phase 16: Analytics API
Body:
Part of EPIC #781

========================================
--- ISSUE 788 ---
Title: Phase 16: Webhook ecosystem
Body:
Part of EPIC #781

========================================
--- ISSUE 787 ---
Title: Phase 16: CHERENKOV Certified
Body:
Part of EPIC #781

========================================
--- ISSUE 786 ---
Title: Phase 16: Multi-org federation
Body:
Part of EPIC #781

========================================
--- ISSUE 785 ---
Title: Phase 16: LLM provider marketplace
Body:
Part of EPIC #781

========================================
--- ISSUE 784 ---
Title: Phase 16: Test template marketplace
Body:
Part of EPIC #781

========================================
--- ISSUE 783 ---
Title: Phase 16: Plugin SDK
Body:
Part of EPIC #781

========================================
--- ISSUE 782 ---
Title: Phase 16: Public API (REST + SDK)
Body:
Part of EPIC #781

========================================
--- ISSUE 781 ---
Title: EPIC: Phase 16 - Platform and Marketplace
Body:
Months 18-30. Goal: CHERENKOV becomes the testing platform layer.

========================================
--- ISSUE 780 ---
Title: Phase 15: Enterprise model hosting
Body:
Part of EPIC #773

========================================
--- ISSUE 779 ---
Title: Phase 15: Model release
Body:
Part of EPIC #773

========================================
--- ISSUE 778 ---
Title: Phase 15: Evaluation harness
Body:
Part of EPIC #773

========================================
--- ISSUE 777 ---
Title: Phase 15: Fine-tuning run
Body:
Part of EPIC #773

========================================
--- ISSUE 776 ---
Title: Phase 15: Dataset curation
Body:
Part of EPIC #773

========================================
--- ISSUE 775 ---
Title: Phase 15: Opt-in corpus collection
Body:
Part of EPIC #773

========================================
--- ISSUE 774 ---
Title: Phase 15: Data pipeline design
Body:
Part of EPIC #773

========================================
--- ISSUE 773 ---
Title: EPIC: Phase 15 - Fine-Tuned Model
Body:
Months 12-18. Goal: Ship cherenkov-coder-7b.

========================================
--- ISSUE 772 ---
Title: Phase 14: Spec change attribution
Body:
Part of EPIC #764

========================================
--- ISSUE 771 ---
Title: Phase 14: Regression detection
Body:
Part of EPIC #764

========================================
--- ISSUE 770 ---
Title: Phase 14: Coverage map
Body:
Part of EPIC #764

========================================
--- ISSUE 769 ---
Title: Phase 14: Auto-regenerate mode
Body:
Part of EPIC #764

========================================
--- ISSUE 768 ---
Title: Phase 14: Alert policies
Body:
Part of EPIC #764

========================================
--- ISSUE 767 ---
Title: Phase 14: Continuous conformance trend
Body:
Part of EPIC #764

========================================
--- ISSUE 766 ---
Title: Phase 14: PR-comment integration
Body:
Part of EPIC #764

========================================
--- ISSUE 765 ---
Title: Phase 14: Spec Guardian daemon
Body:
Part of EPIC #764

========================================
--- ISSUE 764 ---
Title: EPIC: Phase 14 - Spec Guardian
Body:
Months 9-15. Goal: Continuous conformance monitoring.

========================================
--- ISSUE 763 ---
Title: Phase 13: Enterprise support portal
Body:
Part of EPIC #754

========================================
--- ISSUE 762 ---
Title: Phase 13: SLA dashboard
Body:
Part of EPIC #754

========================================
--- ISSUE 761 ---
Title: Phase 13: Bring-Your-Own-LLM
Body:
Part of EPIC #754

========================================
--- ISSUE 760 ---
Title: Phase 13: Compliance report templates
Body:
Part of EPIC #754

========================================
--- ISSUE 759 ---
Title: Phase 13: GDPR compliance mode
Body:
Part of EPIC #754

========================================
--- ISSUE 757 ---
Title: Phase 13: RBAC
Body:
Part of EPIC #754

========================================
--- ISSUE 756 ---
Title: Phase 13: Multi-tenant org management
Body:
Part of EPIC #754

========================================
--- ISSUE 755 ---
Title: Phase 13: SAML 2.0 / SSO
Body:
Part of EPIC #754

========================================
--- ISSUE 754 ---
Title: EPIC: Phase 13 - Enterprise Tier
Body:
Months 5-9. Goal: 5 paying enterprise accounts. $25K MRR.

========================================
