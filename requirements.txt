# CHERENKOV core Python dependencies (CI + local dev).
# Single source of truth for the deps every CI job needs — referenced via
# `pip install -r requirements.txt` so the version set is defined in one place.

# Core data validation
pydantic>=2.7.0,<3.0.0
jsonschema>=4.0.0

# HTTP / networking
requests>=2.32.0,<3.0.0
websockets>=12.0,<14.0

# Config parsing
pyyaml>=6.0.1,<7.0.0

# Web framework (review dashboard + API)
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
python-multipart>=0.0.9,<1.0.0
httpx>=0.28.1,<1.0.0

# CLI
click>=8.4.1,<9.0.0

# Mobile testing
Appium-Python-Client>=3.0.0,<4.0.0

# Optional: Redis session/vector store (graceful fallback to SQLite if absent)
redis>=8.0.1,<9.0.0

# Response-body validation in the spec+prism oracle (graceful fallback if
# absent, but tests/unit/test_oracle_body_validation.py requires it)
# Agent safety & governance (optional — graceful fallback if absent)
# Sponsio: runtime contract enforcement (Apache 2.0)
#   pip install sponsio
# Microsoft Agent Governance Toolkit: policy engine (MIT)
#   pip install agent-governance-toolkit-core
# AgentRR: record/replay debugging (Apache 2.0)
#   pip install agentrr
#
# All three are optional. The chat agent works without them.

# Semantic Memory Layer
memsearch>=0.1.0

# Test tooling
pytest>=9.1.1
pytest-cov>=7.1.0
anyio>=4.13.0

# Templated prompts (GraphQL/accessibility) + CLI completion
jinja2>=3.1.6
argcomplete>=3.3.0
graphql-core>=3.2.0
