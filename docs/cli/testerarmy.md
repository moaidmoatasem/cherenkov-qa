# TesterArmy CLI

The `testerarmy` command group provides project, environment, test, and run management commands.

## Usage
```bash
chernekov testerarmy --help
```

Explore subcommands with `chernekov testerarmy <subcommand> --help`.
