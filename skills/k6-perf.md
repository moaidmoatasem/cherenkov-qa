---
scope: Track B Re-integration
invariants: [D7]
---

# K6 Perf Runner Skill

## Purpose
Run performance load tests and analyze latency metrics using K6.

## Tools
Exposed to MCP via `run_k6_perf`.

## Usage for Agents
Invoke `run_k6_perf` through MCP to stress test endpoints and retrieve latency metrics.
