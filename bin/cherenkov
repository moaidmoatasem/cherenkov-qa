#!/bin/bash
# CHERENKOV CLI Unified Executable Wrappe
#

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
REPO_DIR="$( dirname "$SCRIPT_DIR" )"

export PYTHONPATH="$REPO_DIR"
python3 "$REPO_DIR/cherenkov/cli/core.py" "$@"
